# Chapter 4 · Lesson 2 — dependencies for the LEAN query service.
#
# The query service retrieves context and calls the LLM. It does NOT parse
# PDFs, so it carries none of the heavy Docling / vision stack — that is the
# single biggest size difference from the ingestion image.
#
# (Assumes embeddings are produced via an API provider. If you switch the
#  query path to local embeddings or a cross-encoder reranker, add
#  sentence-transformers here too.)

fastapi==0.115.0
uvicorn==0.30.0
pydantic-settings>=2.10.1
pyyaml==6.0.2
httpx>=0.28.0
slowapi==0.1.9
chromadb>=1.3.5
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.1.0
